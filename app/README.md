# IndexFlow App

Next.js dashboard for the IndexFlow closed-beta URL discovery platform.

## Scripts

```bash
npm run dev
npm run lint
npm run build
npm run start
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` when the API is not running at `http://localhost:4000`.
